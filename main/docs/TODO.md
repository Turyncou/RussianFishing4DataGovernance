# TODO - 从 qt 同步功能

这个文件记录 PySide6 版本 (qt/) 已经新增但 CustomTkinter 版本 (main/) 还未实现的功能，需要后续同步。

---

## 功能列表

### ✅ 已同步

- 暂无

### ⏳ 待同步

1. **饵料/钓具库存统计** - `qt/src/gui/bait_frame.py`
   - 功能：追踪饵料/钓具的购买、使用、剩余库存
   - 需要：在 main 中新增对应的界面和业务逻辑

2. **账号密码加密管理** - `qt/src/gui/credentials_frame.py`
   - 功能：加密存储账号密码，一键复制
   - 需要：在 main 中新增对应的界面和业务逻辑
   - 注意：加密逻辑可以复用 qt 中的 persistence

3. **数据分析增强** - `qt/src/gui/statistics_frame.py`
   - 原有 main 只有基础的三张图，qt 增强为四张图：
     - 每日收益：同时显示当日收益和累计收益两条线
     - 新增：按角色时长分布饼图
   - 需要：同步新的数据分析逻辑到 main

4. **安全增强 - 密码框固定显示****** ** - `qt/src/gui/credentials_frame.py`
   - 当前 main 显示 `*` 长度和密码实际长度一致，会暴露密码长度
   - qt 改为固定显示 6 个星号，更加安全
   - 需要：同步到 main

5. **输入验证和异常处理增强**
   - qt 中添加了更多输入验证，空值、非数字提示更友好
   - 需要：同步这些验证到 main

6. **活动统计增强** - `qt/src/gui/activity_frame.py`
   - 新增功能：查看历史记录（带日期筛选）、导出记录到CSV、从CSV导入历史数据
   - 也包括 `ActivityPersistence` 中新增的 `export_to_csv` 和 `import_from_csv` 方法
   - 需要：同步这些功能到 main

7. **自定义背景功能** - `qt/src/gui/main_window.py` + `qt/src/gui/dialogs/background_settings_dialog.py` + `qt/src/data/persistence.py`
   - 新增功能：支持自定义背景图片，可调节透明度
   - 包含：背景设置对话框、保存设置到配置文件、窗口大小变化时自动重新缩放背景
   - 需要：同步背景功能到 main 版本
   - 注意：需要在 AppSettingsPersistence 中新增保存背景配置的方法

8. **每日计划功能** - `qt/src/gui/daily_task_frame.py`
   - 功能：为每个角色设置每日搬砖/蹲星时长目标，实时跟踪完成进度
   - 活动推荐计算会**优先满足每日计划**，再分配剩余时间给总体目标
   - 需要：在 main 中新增对应的界面和业务逻辑
   - 需要同步：daily_task_frame.py + 数据模型 + 持久化

9. **推荐计算器重构** - `qt/src/gui/suggestion_calculator.py`
   - 功能：新增支持每日计划约束，优化了分配算法
   - 需要：同步新的推荐计算逻辑到 main

10. **对话框模块拆分** - `qt/src/gui/dialogs/`
    - qt 将各个对话框拆分为独立文件：
      - `background_settings_dialog.py` - 背景设置对话框
      - `backup_dialog.py` - 备份恢复对话框
      - `friend_links_dialog.py` - 友情链接对话框
    - main 版本当前内联在主窗口代码中，可以选择同步拆分或保持现状

11. **游戏多启动器** - `qt/src/gui/multi_launcher.py`
    - 功能：管理多个 RF4 游戏快捷方式，支持拖拽添加 `.lnk` 快捷方式
    - 自动检测实例运行状态（未启动/启动中/运行中）
    - 一键启动对应实例，使用 Windows ShellExecute 安全启动，不频繁扫描内存，避免触发反作弊
    - 支持删除实例，配置持久化保存
    - 需要：在 main 中新增对应的界面和业务逻辑

12. **双击全屏/窗口置顶交互优化**
    - 功能：双击标题栏切换全屏，支持窗口置顶显示
    - 置顶后游戏中也能随时看到数据，方便对照
    - 需要：同步交互优化到 main 版本

13. **历史记录查看与导入导出**
    - 功能：支持按日期范围筛选查看历史活动记录
    - 导出全部记录到 CSV，支持从 CSV 导入历史数据
    - 方便数据备份和迁移
    - 需要：同步导入导出功能到 main 版本

---

## 同步完成后请移走到 ✅ 已同步 区域

---

## 🎯 未来功能计划

这些是可以进一步新增的功能，根据需要选择实现：

### 待规划功能

1. **钓点鱼获记录器**
   - 功能：记录出鱼点位坐标、鱼种、重量、是否星鱼、时间
   - 支持按水域筛选，可查看历史记录
   - 价值：方便找到出过星鱼的好点位重复钓

2. **技能熟练度追踪**
   - 功能：记录各个钓鱼技能（手竿/海竿/飞蝇/拟饵等）的熟练度
   - 自动计算每小时增长速度，预测升级时间
   - 绘制熟练度增长曲线

3. **窝料配方制作计算器**
   - 功能：管理窝料/鱼饵配方，计算总成本
   - 和拍卖行价格对比，给出建议（自己做还是买）
   - 支持计算批量制作的总材料需求和总成本

4. **鱼价行情表**
   - 功能：记录不同鱼种在不同水域卖店的价格
   - 查询时自动推荐价格最高的卖鱼地点
   - 支持按鱼种名称快速搜索

5. **成就追踪**
   - 功能：记录游戏内成就完成进度
   - 分类显示已完成/未完成，可添加备注
