# TODO - 新增功能计划

根据讨论整理的功能开发优先级和计划：

---

## ✅ 已完成

- [x] 主页导航（三个大按钮）
- [x] 返回主页按钮
- [x] UI 美化优化配色和间距
- [x] 整理开源 Live2D 模型清单
- [x] **账号密码管理** - 加密存储，点击复制密码到剪贴板
- [x] **自动备份 & 数据导出** - 每次启动自动备份 JSON 数据文件，支持导出全部数据为 Excel，支持从备份恢复
- [x] **饵料/钓具消耗统计** - 动态管理，记录购买/使用/剩余，支持随时增删调整
- [x] **桌面悬浮提醒** - 圆形占位（后续可换 Live2D），定时提醒：零点提醒开加班机，18点提醒开播，饭点提醒吃饭，每小时提醒休息，随机鼓励话语，拖动跟随气泡

---

## 🚀 待开发

### 高优先级

（暂无）

---

## ⏸ 暂不考虑 / 后续再说

---

## ⏸ 暂不考虑 / 后续再说

- [ ] **鱼类图鉴 & 蹲星统计**
  - 原因：鱼类数据经常变更，需要每次从网站爬取，暂时不实现
  - 状态：暂停

- [ ] **钓场收益统计**
  - 原因：可从其他途径获取，暂时不需要
  - 附加考虑：如果能点击直接打开微信特定小程序可以后续考虑
  - 状态：暂停

- [ ] **数据图表可视化**（每日收益趋势图等）
  - 状态：后续再说

---

## 💡 可选扩展（未确定）

- [ ] **每日打卡签到** - 连续打卡记录
- [ ] **闹钟提醒** - 长时间游戏提醒休息
- [ ] **物价换算工具** - 银币 → 人民币自动换算
- [ ] **技能经验计算器** - 计算升级还需要多久
- [ ] **多账号数据隔离** - 支持多个游戏账号切换
- [ ] **音效反馈** - 抽奖按钮点击音效
- [ ] **Live2D 看板娘** - 背景添加互动模型
- [ ] **多主题切换** - 深色/浅色主题
- [ ] **周/月汇总报表** - 每周收益汇总

---

## 完成登记

| 功能 | 完成时间 | 备注 |
|------|----------|------|
| 主页导航（三个大按钮） | 2026-03-24 | 点击跳转对应功能 |
| 返回主页按钮 | 2026-03-24 | 子页面左上角显示返回 |
| UI 美化优化配色和间距 | 2026-03-24 | 统一圆角，调整 spacing |
| 整理开源 Live2D 模型清单 | 2026-03-24 | 保存在 docs/open-source-live2d-models.md |
| 账号密码管理 | 2026-03-24 | 本地 Fernet 加密存储，下拉选择点击复制密码 |
| 自动备份 & 数据导出 | 2026-03-24 | 启动自动备份（保留最近10个），手动备份/恢复/导出 Excel |
| 饵料/钓具消耗统计 | 2026-03-24 | 动态增删，增加/使用库存，显示剩余 |
| 桌面悬浮提醒 | 2026-03-24 | 圆形占位（后续可换 Live2D），定时提醒：零点开加班机/18点开播/饭点吃饭/定时休息，气泡跟随移动，随机鼓励话语可自定义，右键弹出可扩展菜单 |
| PyInstaller 打包配置 | 2026-03-24 | 提供 build.spec 配置文件，支持打包为单文件 exe，可包含自定义字体 |

---

## 📦 打包说明

使用 PyInstaller 打包为 Windows exe：

### 基础打包（使用系统字体）
```bash
pip install pyinstaller
cd main
pyinstaller build.spec
```

打包完成后 exe 输出在 `dist/` 目录。

### 包含自定义艺术字体打包
1. 将下载的 `.ttf` 字体文件放入 `main/fonts/` 目录
2. 修改 `desktop_reminder.py` 中字体路径为 `fonts/xxx.ttf`
3. 打包命令：`pyinstaller build.spec`（已配置自动包含 fonts 目录）

### 推荐开源可爱字体
- [站酷快乐体](https://www.zcool.com.cn/font/ZCOOLQingKeYouHappy-20190428) - 圆润活泼，推荐
- [站酷萌宠体](https://www.zcool.com.cn/font/zcoolmengchongti) - 胖胖圆圆
- [猫啃丸纸体](https://www.maoken.com/freefonts/1252.html) - 软萌手写风

### 完整打包步骤

**1. 安装依赖**
```bash
# 安装项目依赖
pip install -r ../requirements.txt
# 安装 PyInstaller
pip install pyinstaller
```

**2. 确认文件位置**
- 图标文件：`main/芋泥.ico` ✅ 已经在 `build.spec` 中配置好了
- 自定义字体（可选）：放入 `main/fonts/` 目录，会自动打包

**3. 执行打包**
```bash
cd main
pyinstaller build.spec
```

**4. 输出结果**
- 打包完成后 exe 在 `dist/RF4DataProcess.exe`

### 分享给其他电脑使用

打包完成后，需要打包以下内容一起压缩为 zip：

```
RF4DataProcess/
├── RF4DataProcess.exe          # 主程序（打包生成的）
├── 芋泥.ico                     # 图标（可选，如果需要快捷方式）
└── fonts/                      # 如果使用了自定义字体，带上这个目录
    └── your-font.ttf
```

> ⚠️ **注意**：程序第一次运行时，会自动在 `C:\Users\<用户名>\.rf4_data_process\` 创建数据目录存储数据，不需要你提前创建。

### 依赖说明

项目需要的 Python 第三方库都在 `requirements.txt` 中，打包后这些依赖都已经编译进 exe 了，其他电脑不需要安装 Python 或任何依赖，直接双击 exe 就能运行。

